import classNames from 'classnames/bind';
import styles from '@components/detailPage/detailPage.module.scss';
const cx = classNames.bind(styles);

const TworoomDetail = () => {
  return <span>TwoRoom Detail!</span>;
};

export default TworoomDetail;
